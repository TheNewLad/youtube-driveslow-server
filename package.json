{
  "name": "youtube-feed-fetcher",
  "version": "0.1.0",
  "description": "This module fetches a feed from a specified user and returns it in JSON",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [
    "youtube",
    "json"
  ],
  "author": "devslow",
  "license": "ISC",
  "dependencies": {
    "feed2json": "^0.5.0",
    "fetch": "^1.1.0",
    "http": "0.0.0"
  }
}
